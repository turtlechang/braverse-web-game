/**
 * Replay-facing strategy identity. Bump this when Lv.5 decision semantics
 * change so later training jobs can separate comparable decision traces.
 */
export const AI_STRATEGY_VERSION = 'lv5-defense-retention-endgame-v2' as const
