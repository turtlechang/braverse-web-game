# Changelog

本專案的重要變更記錄。格式參考 [Keep a Changelog](https://keepachangelog.com/zh-TW/1.1.0/)。自 `0.9.0`（2026-07-16）起以 git tag 標記發布版本並在此對應版本區塊；更早的項目沿用日期為單位的記錄（未回溯套用語意化版號）。歷史記錄自 README 遷移而來（2026-07-10）。

## [Unreleased]

- 🐛 陷阱雙目標（自身+對手）無法發動修正（2026-07-28）：BS3-021《盾之誓言》同時含 `modify-attack`（`side: 'opponent'`）與 `damage`（`side: 'self'`）兩個 targeted effect，UI 未提供 `selfTargetIds` 時 `validateTrapTargets` 與 `playTrap` 執行端嘗試用對手 cookie 驗證自身目標 → 整張陷阱無法發動。修正 `battle.ts` 四處（`validateTrapTargets`、`playTrap` 的 `damage`/`redirect-attack`/通用 fallback）統一採三層 fallback：① `selfTargetIds` ② `targetIds` 中有效 self 目標 ③ 自動從玩家戰鬥區選取（尊重 `excludeAttackTarget`）。新增 `getTrapSelfTargetCandidates` export。同步補齊 `PlayTrapOptions`/`PlayTrapCommand`/`commands.ts` 的 `selfTargetIds` 欄位，AI `battle-handler.ts` 自動產生，`onlineProtocol.ts` 協定 schema、`battleUiContracts.ts`/`useMatchController.ts`/`useOnlineMatchController.ts` UI 合約、`BattleResponseModals.tsx` 指令組裝、`GameModals.tsx` 導引式 selfTarget 階段（`GuidedPhaseId` 擴充）。新增 `battle-trap.test.ts` 3 項回歸（BS3-021 + BS1-050）；完整單元測試、lint、build 通過（**2,176 項**）。
- 🃏 BS3-025~048 黃色卡牌全數轉換驗證（2026-07-28）：研究 BS3-025~048 全部 24 張黃色卡片的效果轉換路徑與執行器，確認所有效果、技能、攻擊效果、物品／陷阱／舞台能力路徑均正常，無 bug。新增 `bs3-yellow-integration.test.ts`（51 項）覆蓋全部轉換與技能驗證；完整單元測試、lint、build 通過（**2,176 項**）。
- 🐛 BS3-019 靈魂果醬自動跳過裝載步驟（2026-07-27）：使用 BS3-019《靈魂果醬:熱情之光》時，若玩家戰鬥區沒有 BS3-017《聖梅果餅乾》，傷害效果正常結算後，後續的 equip-source 裝載步驟自動跳過，不再顯示 UI 讓玩家手動取消。修改 `commands.ts`（新增 `hasNoEquipTarget` 檢查、`executeAbilityEffects` 與 `resolvePendingAbilityEffect` 跳過邏輯）、`ai/ability-effects.ts`（AI 同步跳過）、`usePendingEffect.ts`（`confirmEffect` 與 `skipOptionalSkill` 跳過）。新增 1 項回歸測試；完整單元測試、lint、build 通過（**141 檔／2,004 項**）。
- 🐛 BS3-017 攻擊後效果無目標時自動略過（2026-07-27）：BS3-017《聖梅果餅乾》的攻擊後效果「Sound the Charge!」以 `excludeSource: true` 排除自己，當戰鬥區只有 BS3-017 一張餅乾時，`min: 0` 的 `modify-attack` 效果因無合法候選仍會顯示 UI。修改 `usePendingEffect.ts` 的 `hasApplicableEffect` 判斷：可選效果（`min: 0`）且無候選目標時自動跳過。新增 1 項回歸測試；完整單元測試、lint、build 通過。
- 🎨 陷阱目標選擇攻擊者視覺辨識（2026-07-27）：陷阱目標選擇步驟新增 `AttackDeclarationSummary` 摘要，讓玩家在選擇目標時同時看到攻擊者與攻擊目標。攻擊中餅乾的按鈕以紅色邊框、深紅實色背景與白色文字明顯標示，下方帶有紅底白字「⚔ 攻擊中」徽章；選中時改為金色邊框與深金背景。修改 `GameModals.tsx`（`is-attacker` 類別與 `attacker-badge`）與 `GameModals.css`（對應樣式）。

- 🐛 實際對局中 `keywords`（靈魂果醬、ancient）從未被賦值的根因修正（2026-07-27）：使用者回報 BS3-009《奶油起司餅乾》支援區明明有 BS3-019《靈魂果醬:熱情之光》，攻擊後卻沒有跳出追加傷害的 UI；用「測試對局設定」開發工具重現時發現該工具的支援區只能放萬用能量張數、無法放置具名卡片，因此改寫隔離腳本直接呼叫遊戲真正使用的建卡路徑重現。根因：`GameCard.keywords` 有兩套獨立的賦值邏輯——`cards/official-card-adapter.ts` 的 `convertOfficialCardToGameCard`（`getRuntimeKeywords`，只有測試與部分匯入流程使用）會正確設定 `keywords`，但 `game/starter-deck.ts` 的 `createCard`（`game/custom-deck.ts` 建立玩家自訂牌組、`game/scenario.ts` 建立測試對局、所有內建起始牌組共用的唯一建卡函式）組出的 `base` 物件完全沒有 `keywords` 欄位。這代表**任何實際對局**（玩家在牌組編輯器建立、使用的牌組）中，凡是依賴 `card.keywords` 判斷的效果都會靜默失效：不只是 BS3-009／BS3-087 這類「支援區至少 N 張靈魂果醬」的攻擊後條件（`support-keyword-at-least`），BS3-121《英雄與王國的時代》的「ancient」特殊勝利條件（`distinct-named-keywords`）同樣受影響。既有測試全數用 `convertOfficialCardToGameCard` 建卡，因此完全沒有測到這個落差。修正：把 `getRuntimeKeywords` 從 `official-card-adapter.ts` 匯出，`starter-deck.ts` 的 `createCard` 改呼叫同一份邏輯計算 `keywords` 並併入 `base`，讓兩條建卡路徑共用單一事實來源。用真正的 `createCard` 路徑寫隔離腳本重現：修正前 `soulJam.keywords` 為 `undefined`、`support-keyword-at-least` 條件恆為 `false`；修正後 `keywords` 正確為 `["soul-jam"]`、條件與目標檢查皆通過。完整單元測試、typecheck、lint、build 通過（**141 檔／2,000 項**）。
- 🃏 P-0XX 促銷卡候選第一切片（2026-07-27，**更正**：原文誤植為 19 張，實際完成 18 張）：新匯入的 `data/candidates/official-promotion-p001-p032.en.json`（P-001~P-032，實際 26 張）完成 18 張的效果轉接，候選仍為 `inventory` 狀態未 promote。已完成：P-001／002／003／013／014（GingerBright 五色，手牌 ≤3 張時被動 +1 攻擊）、P-007（登場時對手 0–1 傷害）、P-008（道具，指定剩餘 HP≥4 篩選）、P-009（攻擊後段：己方休息區等級高於對手才觸發追加傷害）、P-010（登場時對手 LV.1 禁止攻擊到下回合）、P-011（昏厥觸發：支援區取回手牌再放回休息區）、P-012（道具，傷害後自己進支援區橫置）、P-015（攻擊後段可選費用：造成傷害＋捨棄己方餅乾 HP 卡）、P-016（登場時：棄牌區選 1 張進休息區，再從休息區選最多 2 張進棄牌區）、P-018（登場時對雙方戰鬥區造成傷害，排除自己）、P-019（攻擊後段：棄牌區選最多 3 張非 FLIP 洗回牌庫）、P-022（FLIP 抽牌）、P-030（登場時對手全體傷害＋攻擊後段可選費用追加傷害）、P-031（陷阱：棄牌區 15 張以上時對手 LV.3 -1 攻擊並棄 1 張 HP）。新增 4 個引擎原語：`trash-to-break` 效果（棄牌區選餅乾放回休息區，`break-to-trash` 的反向操作，補上 `BreakToTrashEffect.energyColor` 篩選）、`damage-all.excludeSource`（全體傷害排除來源自己，P-018 用來達成「除了這張以外全體 1 傷害」）、`break-level-higher-than-opponent` 條件（己方休息區等級嚴格高於對手）。順手修正兩個既有 regex 過寬的問題：**(1)** `fromBreakArea`（技能只能從休息區發動）原本只要文字裡出現「break area」就誤判，P-016「place them in your break area」只是效果去向、BS3-036／BS1-035／BS1-038 也曾誤判，改成精確比對「this Cookie is in your break area」這個 BS3-025 的前提句式。**(2)** 昏厥觸發偵測只認「When this Cookie faints」，P-011 用「If this Cookie has fainted」，補上第二種措辭。剩餘 8 張（P-017 反應式「支援區卡進棄牌區時」觸發、P-024 HP 卡本身的 FLIP 能力、P-025／026／027 x2 攻擊力倍率＋同名卡數量條件、P-028 場景卡的 `handToBreakArea` 代價尚未接上 `payAbilityCost`、P-029 陷阱的「己方餅乾在本場戰鬥昏厥」延遲判定、P-032 場景卡修改攻擊費用）都需要全新機制，留待下一切片。新增 `p0xx-promotion-cards.test.ts`（25 項）；完整單元測試、lint、build、`validate:candidate`（結構驗證）、`validate:cards`（正式卡池）皆通過（**141 檔／1,997 項**）。
- 🃏 P-0XX 第一切片正式 promote，牌組編輯器可見（2026-07-27）：把候選檔案依完成度拆成兩份——18 張已完成效果轉接的卡片獨立成一份候選、`candidateStatus` 設為 `promotion-ready` 後用 `npm run promote:candidate` 正式搬進 `data/cards/`；剩餘 8 張仍留在 `data/candidates/official-promotion-p001-p032-remaining.en.json`，`inventory` 狀態繼續等待效果轉接。牌組編輯器的系列篩選 `SERIES_OPTIONS` 補上「特典卡」（`PROMOTION CARD`）選項，18 張 P-0XX 卡（含 1 張陷阱 P-031）已可在編輯器中篩選、加入牌組並正確計數，實際在瀏覽器驗證過。`promote-candidate-cards.ts` 對整個 `data/candidates/` 目錄採全有全無驗證（任一檔案仍是 `inventory` 就整批拒絕 promote），因此拆檔案時把仍不完整的候選暫時移出目錄、promote 完成後才移回，過程中沒有修改 `promote-candidate-cards.ts` 本身的邏輯。完整單元測試、lint、build、`validate:candidate`、`validate:cards`（9 個官方檔案、505 種卡號）皆通過（**141 檔／1,997 項**）。
- 🃏 BS3 候選卡正式 promote（2026-07-26）：`npm run promote:candidate` 把 `data/candidates/official-age-of-heroes-and-kingdoms-bs3.en.json`（176 張，含異畫版本）搬進 `data/cards/`、重新生成 `generated-card-pool.ts`，121 張 BS3 基礎卡至此正式進入卡池。Promote 前的完整度檢查揪出兩個真正缺口：**(1)** BS3-095《榮耀奶油共和國》官方文字是「Select 1 of the following」的選擇一項，但原轉接只對到第一分支（查看牌庫頂 3 張重排），且 `restDestination` 誤設為 `'bottom'`（官方文字明確是「放回牌庫**最上方**」）；第二分支「抽 1 張、這張場景卡放到牌庫底」完全沒有對應效果可用。新增 `stage-source-to-deck` 效果（來源場景卡自己離開場景區、送回牌庫頂或底），並把 BS3-095 轉接為完整的 `choose-one`。另外，場景卡的效果對照表原本用含異畫版本後綴的完整卡號（如 `BS3-095@1`）當 key，導致兩個異畫版本完全沒有轉出任何效果——改為以 `baseCardNumber` 查表，讓所有版本共用同一份規則；`convertOfficialStageAbility` 也放寬了「必須有 attackText」的前提（BS3-095@2 這個推廣版本官方資料把安置與啟動文字全塞進 `skill.text`、`attackText` 是 null），並新增 BS3-095 專屬覆寫讓因來源資料缺漏「\<Rest this card.\>」文字的 @1 版本仍套用正確的 `restSource: true`。**(2)** `scripts/validate-candidate-cards.ts` 與 `scripts/lib/card-effect-validation.ts` 的完整度檢查誤判 BS3-121《英雄與王國的時代》「有效果文字但未轉出效果」——這張卡的能力本來就是透過 `specialVictory`（而非 `effects` 陣列）實作的特殊勝利條件，兩份驗證腳本補上 `specialVictory` 的例外；同時修正場景卡橫置判斷（`rest this card`）誤用泛用的 `skill.restSource` 而非 `stageAbility.restSource`。新增 `stage-source-to-deck.test.ts`（6 項，含 BS3-095 三個版本轉接驗證）；9 個測試檔案的候選路徑 import 同步改指向 `data/cards/`。完整單元測試、lint、build 通過（**140 檔／1,970 項**）。
- 🐛 opencode 修改複查：`hasNoLegalSelectableTargets` 中止範圍過廣、split-damage 保護重編號兩個回歸（2026-07-26）：複查 opencode 在本分支追加的 5 個 commit 時發現兩個真正的 bug。**(1)** BS3-019 vs BS3-115 的官方裁定（`hasNoLegalSelectableTargets`）原本只該擋「Select…Then equip」這種選目標效果緊接 `equip-source` 的靈魂果醬寫法，但當時的實作對**任一**選目標效果沒有合法候選就整段中止，導致 BS3-081「對手 0–1 傷害，然後把來源自己送回牌庫頂」這類彼此獨立的兩段效果，在對手戰鬥區剛好淨空時，後段自身效果會被錯誤地一併跳過（結構上同樣影響 BS3-089、BS3-097）。修正為只在下一個效果是 `equip-source` 時才中止，`commands.ts`（`executeAbilityEffects`／`resolvePendingAbilityEffect`）與 `ai/ability-effects.ts`（`simulateAbilityEffects`）兩份判斷邏輯必須同步——先只改 `commands.ts` 曾一度造成 20 項既有測試（`ai-simulation.test.ts`／`bs2-bean-energy-mismatch.test.ts`）拋出「選擇的效果目標數量不合法」，回溯後才發現是這兩份邏輯對「何時中止」不一致，AI 算出的 `effectTargets` 跟正式執行的中止時機對不上。**(2)** BS3-082《勇敢餅乾》`prevent-effect-damage` 保護加入時（709b16d），`split-damage` 執行器把篩掉受保護目標後的陣列重新從 0 編號，若玩家選的第一個目標被保護、第二個沒有，倖存的第二目標會錯拿 `primaryAmount` 而非它原本該拿的 `secondaryAmount`；`damage`／`split-damage` 兩條路徑事後回傳給 `collectAfterDamageEffectsFromIds` 的 `damagedInstanceIds` 也誤用了篩選前的 `targets`（目前因 `damageSource: 'effect'` 會被該函式提早短路而不影響實際行為，但一併修正避免未來變成真正的 bug）。新增 `bs3-then-effects.test.ts` 2 項（含直接呼叫 `simulateAbilityEffects` 驗證 AI 模擬路徑）與 `split-damage-protection.test.ts` 2 項；完整單元測試、lint、build 通過（**139 檔／1,964 項**）。
- 🐛 BS3-006《花小龍餅乾》`modify-all-attack` 缺少顏色與等級過濾（2026-07-26）：官方確認持續效果僅適用於紅色 LV.2 以上餅乾，但 `modify-all-attack` 執行器將 +1 攻擊加成套用至所有己方餅乾。新增 `energyColor` 與 `minLevel` 欄位至 `ModifyAllAttackEffect`，執行器過濾後才建立 modifier；adapter 同步補上 `energyColor: 'red', minLevel: 2`。lint、build 通過。
- ✅ BS3-017《暗黑餅乾》官方 Q&A 核對（2026-07-26）：`modify-damage-received` with `minimumDamage: 3, setDamageTo: 2, duration: 'persistent'`，傷害≥3 改為 2，攻擊與效果傷害皆適用。✅
- ✅ BS3-016《黑莓餅乾》官方 Q&A 核對（2026-07-26）：攻擊附加 `set-active` + `opponent-cookie-fainted-in-current-battle` 條件，戰鬥結束後回到主要階段時啟動。✅
- ✅ BS3-008《巧克力奶油餅乾》官方 Q&A 核對（2026-07-26）：OnPlay `opponent-battle-to-trash` + `destination: 'break'` + `maxLevel: 1`，使暈倒效果正確（HP 卡全數放入棄牌區 + 餅乾放入休息區）。✅
- ✅ BS3-014《黑森林餅乾》官方 Q&A 核對（2026-07-26）：被動 `modify-attack` + `any-battle-area-has-blocker` 條件，雙方戰鬥區任一有阻擋者即滿足。✅
- ✅ BS3-009《奶油起司餅乾》官方 Q&A 核對（2026-07-26）：攻擊 `damage` + `support-keyword-at-least` 條件，不限顏色（五色靈魂果醬任一皆可）。✅
- ✅ BS3-010《奶油起司披薩餅乾》官方 Q&A 核對（2026-07-26）：OnPlay `opponent-battle-to-trash` + `destination: 'break'`，使暈倒效果正確（HP 卡全數放入棄牌區 + 餅乾放入休息區）。✅
- ✅ BS3 官方 Q&A 批次核對（2026-07-26）：7 張卡牌全數正確。BS3-076《草莓可麗餅餅乾》reveal-top-deck peek + 條件 damage 已正確；BS3-060《妖精王餅乾》hp-to-trash 移除 2 HP 後若 HP=0 直接送休息區，source-in-break-area 條件成立時 rest support 已正確；BS3-042《黃金起司護者泥人》battle-to-break 代價先結算、破裂等級先達 10 則先輸；BS3-041《義大利寬麵餅乾》基礎攻擊 3 傷害先結算（對手先暈），battle-to-break 後執行；BS3-040《探險家餅乾》battle-to-break + side: 'either' + maxLevel: 1 雙方戰鬥區 LV.1 選擇已正確；BS3-037《天使餅乾》optional-cost-attack + gain-hp 含 opponent-cookie-fainted-in-current-battle 條件已正確；BS3-029《巴斯克起司餅乾》hand-to-battle 暈倒觸發，技能補充計入離場數量、再登場互斥已正確。
- ✅ BS3-080《濃縮咖啡餅乾》官方 Q&A 核對（2026-07-26）：官方確認效果執行完畢後展示的牌須放回牌庫頂，且僅當條件匹配時才能執行後方效果。`optional-cost-attack` 含 `reveal-top-deck`（`match: { type: 'cookie', energyColor: 'blue', level: 2 }`）+ nested `draw-up-to`，`reveal-top-deck` 以 `deck[0]` peek 不移除牌庫頂卡、條件匹配才執行 nested effect，卡始終留在牌庫頂，行為已正確。新增 1 項回歸測試覆蓋匹配／不匹配場景；`bs3-then-effects.test.ts`（16 項）、lint、build 通過。
- 🐛 BS3-082《勇敢餅乾》`prevent-effect-damage` 執行器 bug 修正（2026-07-26）：官方確認「基礎攻擊傷害以外的傷害來源皆無法對這個餅乾造成傷害」，但 `effectDamagePreventedUntilTurn` 只被寫入、從未在傷害結算時檢查。修正 `damage`、`damage-all`、`split-damage` 三條路徑，受保護餅乾不再承受效果傷害；新增 `isEffectDamagePrevented` 輔助函式。lint、build 通過。
- ✅ BS3-087《濃縮奶油餅乾》官方 Q&A 核對（2026-07-26）：官方確認五色靈魂果醬任一即可滿足條件，且只檢視本次攻擊對象的等級。`support-keyword-at-least` 條件以 `keywords.includes('soul-jam')` 判定不限顏色，`attackTargetOnly: true` + `maxLevel: 1` 鎖定當次攻擊目標 LV.1，實作已正確。
- 🐛 BS3-091《靈魂果醬:真理之光》官方 Q&A 修正（2026-07-26）：**（1）** `inspect-deck` 的 `restDestination` 錯誤設為 `'bottom'`，卡片文字明確寫「未選擇的牌以任意順序放回牌庫**最上方**」，已修正為 `'top'`。**（2）** 官方確認「查看」是玩家自己看（不展示給對手），`inspect-deck` 的 peek 行為已正確。**（3）** 裝載後的攻擊觸發「抽 1 張牌」是**必定發動**而非選擇性，`getEquipAttackEffects` 回傳的 `draw` 效果會直接加入 `attackEffects`，無可選標記，已正確。`bs3-soul-jam.test.ts`（15 項）、`inspect-deck.test.ts`（19 項）通過。
- ✅ BS3-090《光之護佑配劍》官方 Q&A 核對（2026-07-26）：官方確認效果執行完畢後展示的牌須放回牌庫頂，且僅當條件匹配時才能執行後方效果。`reveal-top-deck` 執行器以 `deck[0]` peek（不移除牌庫頂卡），條件不匹配時直接回傳原狀態、匹配時才執行 nested effect，卡始終留在牌庫頂，行為已正確。
- ✅ BS3-092《古典香草花墜飾》官方 Q&A 核對（2026-07-26）：官方確認「雙方戰鬥區每有 1 個 LV.2 餅乾抽最多 1 張」是指檢視雙方戰鬥區後，玩家可自行決定抽 0 至該數量之間的任意張數。既有實作已正確——`draw-up-to-battle-cookie-count` 執行器掃描雙方 `battleArea` 計算匹配 LV.2 數量、乘以 `amountPerCookie` 得到 `pendingDrawUpTo.max`，玩家透過 `resolveDrawUpTo` 自選 0–max 張。新增 1 項回歸測試覆蓋「雙方共 3 隻 LV.2 → 可抽 0–3」場景；`bs3-runtime-effects.test.ts`（17 項）、lint、build 通過。
- 🐛 BS3-115／BS3-019 官方裁定補齊（2026-07-26）：**(1)** 已裝載 BS3-115 的餅乾，攻擊附加效果若以 `attackTargetOnly` 保持同一攻擊對象，不算重新選擇，仍可造成附加傷害。**(2)** 不選擇目標的對手效果（`damage-all`、`field-to-trash-all` 等）與棄置路徑同樣排除受保護餅乾；保護邏輯集中於 `isBlockedByOpponentEffectProtection`。**(3)** BS3-019 等「Select… Then equip」在當場沒有合法目標時，能力效果鏈（含後續 Then 裝載）整段中止，費用與棄牌不回溯；有合法目標卻自願選 0 時 Then 仍可繼續。`bs3-soul-jam.test.ts` 擴充至 15 項；lint／build 通過。
- ✅ 靈魂果醬（Soul Jam）家族官方 Q&A 核對（2026-07-26）：官方確認 BS3-043《靈魂果醬:富饒之光》裝載後的 +2 HP 是一次性效果，之後即使道具持續掛在《BS3-025 黃金起司餅乾》上也不再有任何效果。核對後確認既有實作已正確——`equip-source` 執行器對 BS3-043 只設定 `gainHp`、不設 `attackBonus`，因此不會建立任何持續的 `attackModifiers`。順帶依官方 Q&A 的措辭（「本系列的靈魂果醬都有不同的持續效果或自動效果」）核對了同系列另外四張：BS3-019 的持續攻擊加成、BS3-066／091「該餅乾攻擊時」的自動觸發（`battle.ts` 的 `getEquipAttackEffects` 依裝備卡 id 查表附加進 `attackEffects`）、BS3-115 的「不能被對手選為目標」保護（`targeting.ts` 的 `matchesSelector` 排除候選），五張全數行為正確，只是先前完全沒有測試覆蓋（僅 BS3-019 的裝載機制本身有測試，四張的「裝載後效果」本身皆未驗證過）。新增 `bs3-soul-jam.test.ts`（9 項）補齊這個覆蓋缺口；完整單元測試與 lint 通過（**138 檔／1,949 項**）。
- 🐛 官方 Q&A 卡牌互動修正（2026-07-26）：兩則官方裁定各自對到既有實作的一個真正的 bug。**(1) BS3-100 攻擊後續效果 vs ST3-020 保護**：官方確認「本次戰鬥 HP 不會變 0」的保護（`prevent-knockout`）擋的是戰鬥結束前任何會讓 HP 歸零的移除，不是只擋一般傷害；`hp-to-trash` 執行器原本完全沒檢查 `pendingBattle.preventKnockoutTargetIds`，會讓 BS3-100 的攻擊後續效果無視保護、直接送走最後 1 張 HP 卡。修正時發現並修掉一個更底層的問題：保護生效時把 `removeCount` 算成 0 後，原本的 `hpCards.slice(-removeCount)` 會因為 JS 的 `slice(-0)` 等同 `slice(0)`（`-0 < 0` 為 `false`），把整疊 HP 卡誤判成「被移除」，導致同一張卡同時留在 `hpCards` 又被複製進棄牌區——因此改成保護生效時直接回傳原狀態，不落入減量計算。順手在 `card-abilities.ts` 的 `hpToTrash` 代價支付（`payItem`／`activateStage` 共用）補上同款防禦，雖然目前的候選過濾（`getHpToTrashCostCandidates` 要求 `hpCards.length > untilRemainingHp`）已讓 `removeCount=0` 不可達，仍加上保險。**(2) 再登場順序讓 BS3-028 使 BS3-113 失效**：官方確認回合玩家先執行再登場、非回合玩家的登場條件必須用結算完前者效果後的即時狀態判定；驗證後確認 `replaceDefeatedCookie` 設定的 `pendingOnPlay` 已經正確擋住第二位玩家的 `replace-cookie`，這部分原本就對，僅補上端到端回歸測試鎖住（含放置順序拒絕、`skip-on-play` 自動略過失效的登場效果）。新增 `bs3-official-rulings.test.ts`（10 項）與 3 項 `hp-to-trash` 保護測試；完整單元測試、lint、build 與 AI 瀏覽器驗證通過（**137 檔／1,940 項；20／20、stuck=0**），主 bundle 555.22 KiB raw／145.46 KiB gzip。
- 🐛 BS3-025「至尊國君的復活」整局一次代價修正（2026-07-26）：依官方卡牌 Q&A 釋疑修正——「本場遊戲只能使用 1 次」是**每位玩家**限定一次，即使同一玩家休息區同時有多張 BS3-025，也只共用這一次額度，不是每張實體卡各自算一次。原本 `GameState.skillUsesThisGame` 以 `card.instanceId` 記錄用量，同玩家的第二張複本會被誤判成尚未使用；改用 `playerId:card.id`（同名卡共用、跨玩家互不影響）當 key，`canActivateCookieSkill`／`activateCookieSkill` 同步更新。另外確認 Q&A 提到的第二點——己方戰鬥區與手牌都沒有餅乾時必須從手牌執行「再登場」，不能改用休息區的「至尊國君的復活」頂替——既有的 `pendingReplacement` 一律擋下 `activate` 觸發，本就正確處理，僅補上回歸測試。新增 3 項測試涵蓋同玩家兩張複本共用額度、跨玩家額度互不影響、替補期間無法頂替；完整單元測試與 lint 通過（**136 檔／1,935 項**）。
- 🃏 BS3 候選卡能力第九切片（2026-07-26）：完成最後兩張，BS3 額外能力來源待轉接由 **2 降至 0**——121 張 BS3 基礎卡的能力來源至此全數轉接完畢（主要效果文字 72 → 73）。BS3-046 Golden Cheese Colosseum：`TrapCondition.friendly-color-fainted-this-battle` 新增可選 `minLevel`，改用新的 `PendingBattle.faintedCookies`（含擁有者與等級，`faintedColors` 只留顏色不夠用）判定「己方指定顏色且等級達標的餅乾昏厥」；新增 `AbilityCost.handToBreakArea`（手牌餅乾進自己休息區的代價，會推進 break 等級並走 `resolveBreakLevelVictory`，目前只有 `playTrap` 實作）；延遲觸發成立且效果需要選卡時，`finishBattle` 改把效果轉存進 `pendingAbilityEffect`（`sourceKind: 'trap'`）交給既有逐步結算流程，本機 UI 新增對應的 `useEffect` 從規則層補建 `pendingEffect`。BS3-025 Golden Cheese Cookie：新增 `break-source-to-battle` 效果（來源自己從休息區登場，HP 固定張數，戰鬥區已滿時擋下）、`CardSkill.oncePerGame`＋`GameState.skillUsesThisGame`（整局一次）、`CardSkill.fromBreakArea`；新增 `findSkillSource` 作為唯一的技能來源查找入口（先查戰鬥區，找不到才查休息區），取代 `canActivateCookieSkill`／`activateCookieSkill`／`commands.ts` 兩個 `activate-skill` 分支／本機與線上點擊處理各自查 `battleArea` 的寫法；AI 的三個主動發動決策迴圈改用新的 `getActivatableSkillSources` 納入休息區候選。此卡的原文無 `{mob}`／`{ap}` 標記，新增 `exactCookieSkillTriggers` 覆寫表避免被通用解析器誤判成 `passive`。`inventory` 候選仍禁止 promote；完整單元測試、lint、build、候選資料驗證、正式卡池一致性檢查與 AI 瀏覽器驗證通過（**136 檔／1,932 項；20／20、stuck=0**），主 bundle 555.05 KiB raw／145.41 KiB gzip。

- 🃏 BS3 候選卡能力第八切片（2026-07-25）：補上「選擇一項」與「棄牌區代價」兩個子系統，BS3 額外能力來源待轉接由 **4 降至 2**、主要效果文字已轉接 **70 → 72**。新增 `choose-one` 效果（BS3-068）：這個效果本身永遠不被執行，玩家或 AI 選定模式後由 `expandChooseOne` 就地換成該模式的效果、`effectIndex` 不動，之後每個子效果照常各自走目標選取流程；本機 UI、線上 UI、指令層（新的 `resolve-choose-one`，以及 `begin-*` 指令上的 `chooseOneModes`）與 AI 共用同一份展開邏輯，執行器遇到未展開的 `choose-one` 會直接丟錯而非默默略過。新增 `AbilityCost.trashToDeckBottom` 括號代價（BS3-112）：從棄牌區選 N 張（可限定非 Cookie）放到牌庫底，選取順序即放入順序，代價付不出來時 `canActivateCookieSkill` 直接擋下發動；`trash-to-hand` 同步支援 `cookieOnly` 篩選。剩餘 2 張（BS3-025 休息區發動＋每局一次、BS3-046 戰鬥內延遲觸發＋手牌進休息區代價）的缺口已寫在 `docs/card-effects.md`。`inventory` 候選仍禁止 promote；完整單元測試、lint、build、候選資料驗證、正式卡池一致性檢查與 AI 瀏覽器驗證通過（**134 檔／1,887 項；20／20、stuck=0**），主 bundle 549.55 KiB raw／144.30 KiB gzip。

- 🃏 BS3 候選卡能力第七切片（2026-07-25）：一次補齊跨玩家目標與牌庫操作兩塊缺口，BS3 額外能力來源待轉接由 **12 降至 4**、主要效果文字已轉接 **62 → 70**。新增 `EffectTargetSelector.side: 'either'` 表示官方文字的「either player's battle area」——這種選擇沒有單一擁有者，`getTargetPlayerId` 直接丟錯，效果改用新的 `getCookieOwnerId` 逐一判定並依擁有者分組結算離場（BS3-040 進各自休息區、BS3-076 回各自牌庫頂）。`inspect-deck` 一般化為 `restDestination`（`bottom`／`top`／`trash`）＋ `pickDestination`（手牌／登場）＋ `filterType`／`optionalPick`，同一個彈窗現在也支援「不選任何卡只重排」與「選 1 張登場、其餘棄置」（BS3-083、BS3-114），並順帶修正 `pickCount: 0` 時無法排序也無法確認的既有問題。另新增 `reveal-bottom-deck`（BS3-073）、`hand-to-battle`（BS3-029 昏厥觸發，含 `gainHp`）、`opponent-trash-to-break` 與 `opponent-break-level-at-most` 條件（BS3-028），並讓 `discard-hand`／`draw-up-to-then-discard` 支援 `deck-top` 去向（BS3-088）。共轉接 BS3-028、029、040、073、076、083、088、114。剩餘 4 張（BS3-025、046、068、112）各自需要獨立引擎切片，缺口已列於 `docs/bs3-effect-coverage.md`。`inventory` 候選仍禁止 promote；完整單元測試、lint、build、候選資料驗證、正式卡池一致性檢查與 AI 瀏覽器驗證通過（**133 檔／1,874 項；20／20、stuck=0**），主 bundle 544.25 KiB raw／142.84 KiB gzip。

- 🃏 BS3 候選卡能力第六切片（2026-07-25）：新增四種 runtime 原語——`transfer-hp`（在來源與己方餅乾之間搬移 HP 頂端卡，供牌方 HP 歸 0 時照常昏厥並沿用離場／補位／勝負流程）、`set-cookie-active`（解除餅乾休息狀態，搭配新的 `restedOnly` 目標篩選；與只處理支援區的 `set-active` 分開）、`draw-up-to-battle-cookie-count`（依雙方戰鬥區 LV.N 餅乾數量決定可選抽牌上限，上限 0 時直接略過）與 `trash-to-deck-all`（棄牌區整批洗回牌庫；「Then」以 `thenEffects` 內嵌執行，避免同層效果重掛棄牌區條件後因棄牌區已清空而被重判跳過）。以此轉接 BS3-031、BS3-053、BS3-089、BS3-092、BS3-113，BS3 額外能力來源待轉接由 **17 降至 12**、主要效果文字已轉接 **57 → 62**；UI 文案與 AI 目標排序共用同一份規則（`transfer-hp` 供牌方 HP 不足時 AI 不搬牌，避免自傷送分）。`inventory` 候選仍禁止 promote；完整單元測試、lint、build、候選資料驗證、正式卡池一致性檢查與 AI 瀏覽器驗證通過（**132 檔／1,850 項；20／20、stuck=0**），主 bundle 538.57 KiB raw／141.58 KiB gzip。

- 🃏 BS3 候選卡效果第五切片（2026-07-25）：轉接 BS3-105（對手）與 BS3-113（己方）的牌庫頂最多 1 張送棄牌區攻擊後效果，重用 `deck-to-trash`，BS3 攻擊後 `Then` 覆蓋提升為 **20／26**。`inventory` 候選仍禁止 promote；完整單元測試、lint、build、候選資料驗證、正式卡池一致性檢查與 AI 瀏覽器驗證通過（**131 檔／1,832 項；20／20、stuck=0**）。

- 🃏 BS3 候選卡效果第四切片（2026-07-25）：新增泛用 `battle-area-has-cookie-with-level` 條件與不觸發 Refresh 的 `deck-to-trash` 區域移動；轉接 BS3-086（己方有 LV.3 時棄 1 張對攻擊目標追加傷害）及 BS3-102（雙方各將牌庫頂 2 張送棄牌區），BS3 攻擊後 `Then` 覆蓋提升為 **18／26**。`inventory` 候選仍禁止 promote；完整單元測試、lint、build、候選資料驗證、正式卡池一致性檢查與 AI 瀏覽器驗證通過（**131 檔／1,830 項；20／20、stuck=0**）。

- 🃏 BS3 候選卡效果第三切片（2026-07-25）：通用化可選攻擊後目標流程，依子效果顯示己方／對手與最小目標數量，支援「最多選 1 個」的 0 目標確認；轉接 BS3-033（剩 1 HP 對手進休息區）、BS3-088（棄 1 張後己方 +1 HP）與 BS3-101（剩最多 2 HP 對手進棄牌區），BS3 攻擊後 `Then` 覆蓋提升為 **16／26**。`{mou}` 附著仍因未確認的生命週期規則而維持候選；完整單元測試、lint、build、候選資料驗證與正式卡池一致性檢查通過（**131 檔／1826 項**）。

- 🎨 戰鬥區卡槽與提示版面校正（2026-07-24）：中央「戰鬥區」文字固定；單張餅乾落在左槽，雙張餅乾以加大間隔分居左右槽。HP dock 移至卡片下緣並放大；能量不足與啟動技能改為置於卡片外側垂直中線（左卡向左、右卡向右），本機與線上共用 `BattleRow`。新增卡槽與提示定位回歸；`npm test -- --maxWorkers=1`、lint、build 與 AI 瀏覽器驗證通過（125 檔／1794 項、20 場完成、stuck=0）。

- 🎨 線上戰場桌機版面收斂（2026-07-20）：對手手牌改為貼齊頂緣的淺弧牌背；雙方戰鬥區、支援區與休息區改採同高鏡射排列，對手區域統一紅色邊框、我方區域統一藍色邊框，並移除共享戰鬥區中央邊線。支援數量文字與資源區對齊同步調整，`table-status-banner`／`RemoteActionBanner` 不再掛載於 `BattleTable` 以避免遮蔽戰場，`PhaseRail` 改為深藍底、金色外框。測試基線升至 **124 檔／1771 項**；`npm test`、`npm run lint`、`npm run build` 通過，主 bundle **520.53 KiB raw／137.38 KiB gzip**（僅保留既有超過 500 KiB 的建置警告）。
- 🎨 `/?mockup=battlefield` 同步大卡圖 hover 浮窗改版（2026-07-19）：`BattlefieldMockup.tsx` 移除常駐的 `mock-bf-preview-rail` aside（原本佔用固定 180px 左欄），改為 hover 才渲染、寬度維持 180px 的浮動面板，行為與實際戰場的 `CardPreviewPanel` 一致；主要區域改用全寬版面。
- 🎨 戰場版面重新設計－參考 Master Duel 排版的第三輪調整（2026-07-19）：**大卡圖預覽改為純 hover 浮窗**：`CardPreviewPanel` 不再是常駐佔用左欄寬度的元件（無 hover 時完全不渲染，取消原本的「Hover Preview」佔位提示），改成 hover 時才顯示、疊在畫面左側的浮動面板（`min(300px,24vw)`，位置貼近左上角），視覺樣式沿用既有深藍玻璃風格；`.table-area` 因此不再需要保留任何側欄寬度，戰場本體改用全螢幕寬度（移除 `--side-panel-width` 變數與所有相關 calc）。**手牌尺寸適度放大**：`.hand-card` 寬度從 112px 提升到 132px（各響應式斷點依比例同步放大），連動調整先前為手牌局部露出（對手 1/3、我方 1/2）所計算的開窗高度與補償位移，確保裁切邊界仍對齊卡片新尺寸；過程中發現 800px／620px 高度斷點先前縮小卡片尺寸時沒有對應調整開窗參數，一併修正。**玩家/對手角落資訊卡新增佔位頭像**：`row-meta` 新增 `.row-avatar` 圓形徽章，內容為玩家名稱首字母（無頭像素材，先以文字佔位），桌機顯示、行動裝置隱藏（維持既有精簡版面）。測試基線維持 123 檔／1738 項，含更新的 `BattleTable.test.tsx`／`InteractionOverlays.test.tsx` 斷言。

- 🎨 戰場版面重新設計－第二輪實機回饋校正（2026-07-19）：三項追加調整。**`PhaseRail` 改為 `table-divider` 子元素**：從獨立浮動區塊（相對 `.game-shell` 定位）改為巢狀在 `.table-divider` 內（`.table-divider` 新增 `position:relative`），並改為相對它 `right:0` 置中定位，視覺上仍是貼近中央分隔列、垂直置中且會溢出到相鄰欄位的小區塊；行動裝置（<900px）改用 `position:fixed`（因巢狀後改用 `.table-divider` 而非全螢幕作為定位容器，`inset` 版面需要脫離巢狀關係才能維持滿版頂部橫條）。**`turn-indicator` 內容對調並移除重複的 `turn-counter`**：上排文字從玩家/對手名稱改成回合數（`TURN N`），下排維持顯示目前階段；原本另外顯示回合數的 `.turn-counter` 元素已移除（避免重複），`PhaseRail` 少一排內容後總高度縮小，與相鄰欄位的重疊範圍也隨之縮小。`PhaseRailProps` 移除不再使用的 `activePlayerName`。**`row-meta` 角落資訊卡依對手/我方左右鏡射**：對手固定於（各自戰鬥區）右側、我方固定於左側（新增 `.bottom-field .row-meta` 左側定位覆寫，含邊框/陰影方向翻轉，桌機與行動裝置斷點皆已對應調整），讓玩家名稱與同側的休息區等級資訊視覺上落在同一角落。測試基線維持 123 檔／1738 項，`PhaseRail.test.ts`／`BattleTable.test.tsx`／`BattleHud.test.tsx` 斷言同步更新。
- 🎨 戰場版面重新設計－依實機預覽回饋校正 PR-1/2/3/4（2026-07-19）：使用者檢視 PR #72 部署預覽後，對照原始線稿圖修正四項落差。**PR-2 還原**：`BattleRow` 支援/戰鬥區改回上下堆疊（先前誤解為橫向並列），移除對應的 `combat-slots` 間距/`grid-row` 覆寫；休息區「×N」樣式維持不變。**PR-3 還原**：`.utility-zones`／`.break-zone` 改回依對手/我方左右鏡射（先前誤統一為雙方右欄），資源彈出視窗開啟方向規則同步還原；`row-meta` 重複數字移除維持不變。**PR-1 重做**：`PhaseRail` 不再是全高右欄，改為垂直置中的小區塊（`top:50%`+`transform:translateY(-50%)`），定位在對手休息區（右上）與我方牌庫/場景/棄牌欄（右下）之間，貼近中央分隔列；`.table-area` 不再為它保留獨立欄寬。實測確認此區塊會與相鄰欄位的外緣留白略有重疊，但兩欄位置中的實際可視內容（圖示/文字，皆置中對齊）都在重疊範圍之外，不影響點擊。**PR-4 加做**：手牌改為只露出部分高度（`overflow:hidden` 開窗，對手手牌露出上方 1/3、我方手牌露出上方 1/2，其餘裁切於容器外），置中對齊維持不變；玩家手牌因沿用 `bottom:0` 定位的既有 CSS，額外加上等量負值 `bottom` 補償量才能讓露出的是卡片上半部而非下半部，開窗高度已對齊各既有斷點（1500px+／800px／720px 高度）各自的手牌尺寸重新換算。測試基線維持 123 檔／1739 項，含更新的 `PhaseRail.test.ts` 斷言。
- 🎨 戰場版面重新設計 PR-4／4：手牌置中 + 行動按鈕集中右下角（2026-07-19）：`.hand-fan.bottom-hand` 從貼右改為在戰場區內置中對齊（`left:50%`+`translateX(-50%)`，寬度上限放寬到 680px），行動裝置（<900px）維持改版前的貼右版面（含修正一個 transform 未重設導致手牌位置偏移的問題）。`MatchToolbar`（重置/查看牌組/暫停）與 `BattleLogSidebar` 開關從左上/右上角移到右下角，與 PR-1 已搬到右側的「結束回合」按鈕群聚，彼此不重疊；`BattleLogSidebar` 展開面板同步調整錨點，行動裝置版面維持改版前的右上角定位不變。玩家 LV／角色資訊維持現有 `row-meta`／`break-zone` 位置，未另外新增角標元件。四階段戰場改版至此全部完成。測試基線維持 123 檔／1739 項。
- 🎨 戰場版面重新設計 PR-3／4：牌庫／場景／棄牌統一右欄（2026-07-19）：`.utility-zones`（牌庫/場景/棄牌）與 `.break-zone`（休息）不再依對手/我方左右鏡射，統一為休息在左欄、牌庫/場景/棄牌在右欄（雙方垂直鏡射：對手右上、我方右下）；資源彈出視窗的開啟方向規則同步簡化為「右欄一律往左開」，已實測確認不會超出畫面。`row-meta` 角落資訊卡移除與牌庫/棄牌/休息重複的數字（`row-status` 現在只保留狀態與手牌數，避免同一數字出現兩處），對應行動裝置斷點的死樣式一併清除。測試基線維持 123 檔／1739 項（2 項既有測試斷言同步更新以反映移除的重複欄位）。
- 🎨 戰場版面重新設計 PR-2／4：支援／戰鬥區橫向並列（2026-07-19）：`BattleRow` 的 `field-stack` 從「支援 45%／戰鬥 55% 上下疊」改為橫向並列（支援｜戰鬥），取代原本上下堆疊的版面；支援卡/戰鬥卡定位邏輯、休息區與牌庫/場景/棄牌欄位的鏡射位置維持不變（休息區/牌庫欄位的左右鏡射留待 PR-3 處理）。休息區數量徽章改為「×N」樣式，既有的休息區逐卡效果目標點擊、彈出視窗功能完全不變。行動裝置（<900px）版面維持改版前的上下堆疊行為（新增 `grid-row` 覆寫確保戰鬥區仍緊鄰中央分隔列）。測試基線維持 123 檔／1739 項。
- 🎨 戰場版面重新設計 PR-1／4：左欄卡片預覽 + 簡化階段列（2026-07-19）：依新戰場線稿圖全面取代 `docs/ui-reference/01-battlefield-wireframe.md` 的第一階段。左欄從 `PhaseRail` 改放整併後的單一 `CardPreviewPanel`（取代原本角落的兩個 208px 小面板），依「目前 hover 的卡」優先、無 hover 時退回顯示對手行動預覽，無卡可顯示時顯示常駐的「Hover Preview」提示區塊而非直接消失；`PhaseRail` 移到右欄並大幅簡化：移除 5 階段進度列表、逐階段提示文字與品牌 logo，只保留「目前階段」（我方回合藍底、對手回合紅底）與既有的動態「下一步」按鈕。`.table-area` 改成同時扣除左右兩欄寬度。行動裝置（<900px）版面：`PhaseRail` 維持轉為頂部橫條、卡片預覽維持隱藏，行為與改版前一致。測試基線維持 123 檔／1739 項。
- 🎨 抽牌選擇、AI 公開卡牌與對戰資訊佈局調整（2026-07-18）：`draw-up-to` 效果 `max === 1` 時（如 ST4-007「若手牌 6 張以下可抽 1 張」、ST4-021「…之後可抽 1 張」等所有同型卡）不再彈出「不抽／抽 1 張」選擇框，直接自動抽取（牌庫為空則自動略過），本機與線上共用同一個 `PendingDecisionModals` 判斷點；本機對戰 AI 單張卡牌公開（如 FLIP）改為不暫停等待玩家點擊確認，資訊改由 toast 訊息帶出（AI 多張棄牌公開仍維持原本的暫停確認，兩者風險不同）——此為實驗性調整，先移除確認框觀察體感；對戰桌版面調整為新預設佈局：對手動作大卡圖預覽（`CardPreviewPanel.is-top`）固定貼左，雙方 OPPONENT／PLAYER 資訊徽章（`BattleRow` 的 `.row-meta`）固定貼右，兩者互換位置，玩家自己手牌 hover 預覽（`.is-bottom`）不受影響仍貼右。測試基線升至 123 檔／1740 項。
- 🎯 攻擊可行性提示（2026-07-17）：雙人線上實測發現能量不足時點擊自己的餅乾會靜默退回卡牌詳情，玩家無從得知是能量問題還是功能異常。`BattleRow` 新增：主要階段能量足夠可攻擊的餅乾套用 `is-attackable` 金色光暈（沿用既有 `is-selected`／`is-targetable` 視覺語彙）；能量不足時顯示「能量不足：需要 X，目前可用 Y」提示。純前端提示與樣式變更，不影響規則層攻擊判定；已於雙瀏覽器線上對局實測兩種真實不足情境（3 需/0 有、2 需/1 有）確認提示正確顯示；測試基線升至 119 檔／1694 項。
- 🧹 對戰可視化 P1／P2 收尾補強（2026-07-17）：`actionProgress` 的能量支付步驟標籤由「費用」改為「能量」，與導引式 `EffectPanel` 用詞一致；線上活動紀錄面板新增「複製紀錄」按鈕，重用既有 `ReplayIssueBundleV1` provider 產生可貼給開發者重現的問題包；移除從未被觸發、伺服器也無對應能力的 `reconnecting` 連線狀態死碼（型別、`RemoteActionBanner` 文案與樣式），斷線統一走既有「請重新加入房間」流程。`docs/known-risks.md` R7 補充說明伺服器無 session token／房間狀態保留機制，重連恢復與 45 秒決策逾時強制執行仍為此風險解除時才一併設計。新增活動紀錄複製功能回歸測試；測試基線升至 119 檔／1694 項，主 bundle 509.75 KiB raw／133.27 KiB gzip。
- 🐛 線上互動缺口修正（2026-07-17）：`useOnlinePendingEffect` 補上 `gain-hp` 帶目標選擇器時（如 BS1-052）的候選卡推導，避免目標階段永遠空白而無法確認發動；`useOnlineMatchController` 補齊陷阱 `support-to-hand`／`hand-to-support` 候選清單與 toggle（此前為佔位存根，BS2-021 等連鎖效果在線上會被靜默跳過）；共用 `BattleRow` 的懸停/聚焦放大預覽從只涵蓋戰鬥區餅乾，擴大到支援區、休息區、手牌、場景卡與棄牌堆頂卡。同步修正 `ai-browser-validation.mjs` 對 `RemoteActionBanner`（現為 `<aside>` 根節點）的中央指引選擇器誤判。新增對應回歸測試；測試基線升至 119 檔／1692 項，主 bundle 522.08 KiB raw／137.54 KiB gzip。
- 👁️ 對戰可視化 P0–P2 收尾（2026-07-17）：中央提示統一補齊玩家／階段／來源卡、等待原因、五步驟進度、攻擊箭頭與事件句型；加入對手公開卡牌預覽、陷阱／FLIP／攻擊效果一致回應狀態；活動紀錄支援回合／階段／玩家／卡牌篩選，線上同步顯示已收到對手操作、拒絕／斷線狀態與伺服器提供的 45 秒決策期限。所有公開提示沿用伺服器過濾後的卡牌 instance ID，不揭露對手手牌或牌庫；測試基線升至 119 檔／1684 項，主 bundle 507.67 KiB raw／132.89 KiB gzip。
- 🪙 攻擊能量支付互動修正（2026-07-17）：線上戰場下方 `BattleRow` 補上攻擊支付候選清單，BS1-007 等中性攻擊費用可直接點選支援卡；本機與線上共用規則層的中性費用候選判定，並新增線上互動與無顏色支援卡回歸測試。測試基線升至 119 檔／1687 項，主 bundle 507.58 KiB raw／132.88 KiB gzip。
- 🧭 BS1-037 攻擊後效果提示修正（2026-07-17）：本機與線上共用合法目標判定，只有存在對手 LV.1 餅乾時才建立攻擊後效果提示；無合法目標或條件不成立時由規則層自動略過，已有提示可使用「略過」，來源卡與效果文字整合在同一個 `EffectPanel`。新增 BS1-037 高等級目標與空戰鬥區回歸測試；測試基線升至 119 檔／1689 項，主 bundle 509.25 KiB raw／133.22 KiB gzip。
- 🧭 效果提示流程修正（2026-07-17）：ST3-019 在既有陷阱回應框內讓玩家選擇支援區棄牌；BS2-021 目標卡改為可換行並支援垂直捲動；只有「確認發動」的效果面板按鈕改為右側緊湊寬度；BS2-044 攻擊可選效果整合至本機與線上共用的單一提示框，完成代價與目標後才送出指令。
- 🎨 主選單品牌標題（2026-07-17）：主畫面標題改用 CookieRun BRAVERSE 金色／棕色品牌文字排版，保留響應式尺寸、可存取標題與既有選單操作。
- ⚡ 能量顏色支付修正（2026-07-17）：`MIX` 能量卡若有明確 `color`，支付時依該顏色判定；修正 BS1-032、BS1-007 等卡牌在 BS2-015／BS2-018 費用中被誤列為萬用能量，並同步阻擋玩家與 AI 選取不合法的攻擊支付卡。測試基線升至 116 檔／1674 項。
- 👁️ 對戰動作可視化第一版（2026-07-16）：本機與線上共用 `ActionStatus`／`RemoteActionBanner`，線上由伺服器維護具序號與 `stateVersion` 的 `PublicIntent`，只投影公開區域牌面；攻擊、技能／OnPlay 導引、陷阱／FLIP 回應與效果結算會同步目前步驟、對手選取目標高光及非阻塞等待提示，成功 `GameCommand` 後自動清除意圖並保留既有 commandLog／活動紀錄。

## [0.9.0] - 2026-07-16

- 🛡️ R5 卡牌語意防線（2026-07-16）：`validate:cards` 從「可轉換／有任一 payload」提升為 ability 非空、技能標記、可選抽牌與來源橫置檢查，並以 8 張高風險卡契約鎖定 Then／If you did／特殊代價／條件／觸發。整合 2026-03-30 官方 Rule Update，新增同時補位逐一處理 OnPlay 與傷害步驟開始後不回溯傷害的完整流程回歸；R5 保留為官方規則持續更新風險，測試基線升至 115 檔／1663 項。
- 🃏 ST5-022 觸發修正（2026-07-16）：改以被效果送入棄牌區的餅乾所屬玩家判定對手戰鬥場事件；己方 ST5-007 丟棄對手 LV.1 餅乾後，現在會正確提示是否橫置己方 ST5-022 並抽 1 張牌，且不會因己方餅乾被丟棄而誤觸發。新增完整技能支付與目標選擇回歸，測試基線升至 113 檔／1657 項。
- 🌐 好友房開局整合（2026-07-16）：加入對手後直接進入戰場背景，由伺服器權威狀態機依序處理私密猜拳、同時揭曉、勝者選先後攻、先攻再後攻調度、無餅乾公開與補償、雙方私密選擇並同步揭示起始餅乾；一般對局指令在完成前會被拒絕。開局浮層顯示雙方提交進度與目前行動者，正式戰場持續顯示先攻／後攻，中央狀態列明示自己或對手目前階段。雙瀏覽器 Playwright 已驗證內容保密、完整開局、順位／階段同步及既有斷線負向路徑；測試基線升至 113 檔／1656 項，主 bundle 749.50 KiB raw／156.56 KiB gzip，維持 850／180 KiB budget 內。
- 🛡️ R15 收尾（2026-07-16）：線上 `useOnlineMatchController` 補齊陷阱能量付款與 BS2-079 `trash-to-deck` 候選推導，玩家可在「目標」步驟分別選擇攻擊者降攻及最多 5 張自己的非 FLIP 棄牌，並透過既有 `play-trap.trashToDeckIds` 送交權威伺服器；非法顏色、橫置支援、FLIP 棄牌與第 6 張選擇均不會進入命令。協定回歸另確認非字串 `trashToDeckIds` 會被拒絕。全卡池只有 BS2-079 需要此專屬欄位，R15 正式解除；測試基線升至 112 檔／1643 項。
- 🧩 卡牌效果修正（2026-07-16，fix/card-effects-gaps）：split-damage 列舉所有合法配置 [A]/[B]/[A,B]/[B,A]，index 0 套 primaryAmount、index 1 套 secondaryAmount，依昏厥數→殘留 HP 排序；補強 draw-up-to-then-discard 在 afterEffectsRequireDraw 條件下的 pendingDrawUpTo 早返行為、draw 0/1 回歸測試（3 項）。isEffectTargeted 補齊 split-damage、prevent-effect-damage；AI 效果目標選擇新增 split-damage（5 項，含 HP1+HP2 2+1 回歸）與 hp-to-trash/support/disable-flip/disable-attack/battle-to-support/prevent-effect-damage（8 項）回歸測試。測試基線升至 112 檔／1641 項；AI 瀏覽器 20/20 種子 stuck=0 全綠。
- 🎨 本機與線上效果操作統一為導引式流程：餅乾、物品、場景與陷阱共用「能量 → 代價 → 目標」phase-step，缺少的步驟會自動略過；每次只顯示目前步驟，提供「下一步／上一步」，並只在最後以「確認發動」提交。技能文字移到來源卡圖右側，手機版同步調整卡片摘要與陷阱操作列。線上 `begin-activate-skill`／`begin-play-item`／`begin-activate-stage` 可在最後一次命令一併帶入第一段效果目標，避免導引途中先改動權威狀態；測試基線升至 110 檔／1625 項。
- 修正餅乾 OnPlay 登場提示：本機與線上對戰在效果尚未發動前皆提供取消按鍵；線上 BS2-061 現可顯示棄牌區非 FLIP 候選卡、選擇最多 3 張並正常確認洗回牌庫。
- 🌐 線上對戰互動與身份補強（2026-07-15）：建房／加入房間新增 1–20 字元玩家名稱並由伺服器保存至雙方 `GameState`；補齊線上戰場點擊手牌外部或按 `Escape` 取消選取；新增經伺服器場面 ID 過濾的非權威攻擊選取預覽，讓對手即時看到攻擊餅乾高光與付款支援卡橫置，正式攻擊仍由既有 `declare-attack` 裁決；修正付款完成後無法點選攻擊目標、BS2-069 類「手動代價＋必選目標」費用階段無法確認、ST4-017 移至棄牌區後來源卡圖片降級成 `unknown` 與重複送出結算；線上戰場補齊牌庫、場景區、休息區提示及棄牌區清單。擴充協定、RoomStore、hook、元件與雙瀏覽器回歸，測試基線升至 109 檔／1622 項。
- 🛡️ Phase 2 問題包（ReplayIssueBundleV1，2026-07-12）：新增 `src/game/replay-issue-bundle.ts`——版本化問題回報格式，含 bundleVersion、種子、牌組識別、完整 commandLog、回合/階段、錯誤摘要、失敗指令（失敗指令不落 commandLog，獨立欄位才能重現錯誤）、對局起點快照（`initialState`，離線限定）與擷取當下狀態（`capturedState`）。線上模式一律經 `maskGameStateForViewer` 遮罩再輸出、`initialState` 強制 null，序列化結果不含對手手牌/牌庫/隱藏 HP 卡的任何 instanceId（字串層級測試保證）。UI 入口：暫停選單與 `GameErrorBoundary` 崩潰畫面各加「複製問題包」按鈕（崩潰路徑靠 `issueBundleSource` provider registry 保留最後對局狀態，避開 React 卸載順序問題）；`useMatchController` 的 dispatch 失敗時記錄失敗指令供問題包附帶。回歸測試四組：離線 initialState+commandLog 重播出相同終局（黃金重播）、線上遮罩保證、serialize→parse round-trip、以及「重現真實錯誤案例」——漏付 trashBattleCookie 代價的 play-trap（BS2-077/ST5-020 類真人試玩 bug）經問題包 parse 後重放可得一模一樣的錯誤訊息，對應 Phase 2 退出條件「問題包能重現至少一個真實錯誤案例」。已知限制：未帶種子的洗牌指令重播時牌序會分岔（精確重現以 capturedState 為準）；線上端牌組識別為 unknown、失敗指令待 R14（command-rejected 回流 UI）後補。瀏覽器實測暫停選單複製成功回饋與 console 無錯誤；測試基線升至 101 檔／1591 項。
- 🎨 Phase 1 設計 token 與主題變體系統（2026-07-12，接手 Codex 本地未提交工作並驗證交付）：新增 `src/styles/`（`tokens.css` 集中色彩/字型/間距/圓角/陰影/動畫/z-index 設計原子、`base.css` 基礎樣式、`themeStorage.ts` localStorage 持久化、`themeQuery.ts` 網址參數覆寫、`ThemeSwitcher.tsx` 切換元件，含單元測試）。5 個視覺變體：`tactical`（原版電競科幻，對照組）、`tactical-clean`（**預設**，使用者反映原版太花俏後的資訊優先版）、`tactical-mono`（純灰階對照組）、`low-glare`（夜間/OLED）、`broadcast`（錄影/直播放大字級）。`main.tsx` 開機依 `?theme=` 參數或 localStorage 套用 `data-theme`；`/?mockup=themes` 提供 5 變體並列預覽。新增 `docs/phase1-theme-variants.md`／`docs/phase1-state-matrix.md`（5 畫面狀態矩陣）／`docs/phase1-component-states.md`（9 類元件狀態規範）。已於瀏覽器驗證 mockup 頁 5 變體渲染、主程式預設 `tactical-clean` token 生效、`?theme=broadcast` 覆寫生效、console 無錯誤。`ThemeSwitcher` 元件已就緒但尚未掛載進主選單（待使用者確認視覺方向後接入）；測試基線升至 100 檔／1582 項。
- 🃏 牌組編輯器補齊物品卡／陷阱卡／場景卡數量提示：原本 stats 區塊只顯示 `FLIP` 與 `餅乾卡` 兩項（`src/components/modals/DeckEditorModal.tsx` 與 `src/components/MainMenu.tsx`），現在於牌組編輯器 modal 與主選單「目前玩家牌組」／「已儲存牌組」三處皆新增 `物品卡`／`陷阱卡`／`場景卡` 提示。純資訊顯示、不加任何驗證錯誤規則；新增 `DeckEditorModal.interaction.test.tsx` 1 項 stats bar 顯示回歸，測試基線升至 98 檔／1569 項。
- 🃏 牌組編輯器將 `@1` 卡面變體視為同名卡：`BS2-031@1` 與 `BS2-031` 共用 4 張上限；卡池列表只顯示 base 條目，匯入既有牌組自動正規化為 base；`src/game/card-pool.ts` 新增 `normalizeCardNumber` 與 `getCardPoolVariants` 公開 API，`useDeckEditor` 與 `custom-deck` 同步改寫。受影響 base 卡牌：BS1-002/003/008/009/012/014/017/028/031/033/036/037/040/044/053/054/056/062/066/067/071（21 張）、BS2-003/011/015/022/026/027/028/029/031/034/036/040/045/055/058/061/062/063/067/068/069/071/073（23 張）；同步把 `AI_PRESET_BS2_PURPLE_DECK` 中硬編碼的 `BS2-068@1` 改為 `BS2-068`；新增 `card-pool.test.ts` 5 項與 `custom-deck.test.ts` 4 項回歸，測試基線升至 98 檔／1569 項。
- 🐛 紫色卡牌全面稽核（2026-07-12，資料轉換／hook／UI／規則引擎四層檢查）：發現並修復 FLIP 卡的頂層 `effectText`/`effects` 未填入問題——`official-card-adapter.ts`／`starter-deck.ts` 的 fallback 鏈（`trap → item → stageAbility`）漏了 `flip` 分支，導致通用轉換器無法解析的 FLIP 文字（如 BS2-056「棄 1 張手牌 → 該餅乾 HP +1」）只有 `card.flip` 有值，`CardDetailModal` 的 FLIP 段落因此不會渲染（純顯示層，規則引擎只讀 `card.flip` 不受影響）。已於兩處 adapter 補上 `flip` 分支，新增 adapter 層與元件層回歸測試，並實際在瀏覽器測試對局模式載入 BS2-056、點擊卡牌確認 FLIP 段落正確顯示。另盤點 `'opponent-trash-count-at-least'` 條件命名：`TrapCondition`（`battle.ts` 判定，檢查陷阱擁有者自身棄牌區）與 `EffectCondition`（`targeting.ts` 判定，檢查真正對手棄牌區）語意相反卻同名，目前 BS2-080 因呼叫方式巧合而運作正確，非活躍 bug，但屬維護地雷；已加鎖定回歸測試與程式碼註解說明，不貿然重新命名。記錄為 known-risks R16（已解決）；測試基線升至 97 檔／1554 項。
- 🛡️ R15 第一階段緩解：BS2-079 陷阱「trash-to-deck」效果新增獨立 `trashToDeckIds` 目標欄位（2026-07-12，PR #47）。陷阱系統只有單一共用 `targetIds`，第二段可選目標效果先前因誤用第一段目標 ID 導致例外；本輪已貫穿 `battle.ts`／`commands.ts`／AI／本機 UI，線上 UI 留待 2026-07-16 的 R15 收尾項目完成。
- 🐛 真人試玩回報修復（2026-07-12，[manual-playtest-checklist.md](docs/manual-playtest-checklist.md) 試玩紀錄）：
  - 線上對戰攻擊方在對手決定陷阱/Blocker/FLIP 時無任何等待提示，體感等同卡死；`OnlineBattleView.tsx` 狀態列新增 `opponentDecisionLabel`，涵蓋 trap/flip/attack-effect 三種待決策階段。
  - BS2-077 `trashBattleCookie` 物品代價完全未執行就結算效果：補齊 `PlayItemCommand`／`playItem()`／`payAbilityCost`／AI `chooseAbilityCostIds`／人類互動流程 `begin-play-item` 的欄位與邏輯，新增回歸測試。
  - BS2-079 陷阱「棄牌洗回牌庫」後續效果從未轉出：`official-effect-adapter.ts` 補上 `trash-to-deck` 效果；發現陷阱系統只有單一共用 `targetIds`（無法比照物品/技能逐效果選目標），已避免例外崩潰但效果本身仍是靜默無選擇，記錄為新風險 known-risks R15。
  - BS2-058「攻擊後續效果邏輯錯誤」複現後查出實為 UI 顯示錯誤文字：`EffectPanel.tsx` 誤讀 `sourceCard.effectText`（卡牌固定技能文字）而非 `pendingEffect.skill.text`（依當下情境正確設定），導致攻擊後續效果提示框顯示成 OnPlay 技能文字。條件/目標/傷害邏輯本身正確，已修正顯示欄位並補回歸測試；另新增 `battle-attack-effect.test.ts` 端到端整合測試，實跑「攻擊 → 主傷害 → 條件判定 → 後續傷害」全流程驗證攻擊方棄牌區達 15 張前後兩種結果；並補上 `usePendingEffect.test.tsx` hook 層測試，用真實 attack-effect `pendingBattle` 狀態驗證 hook 給 UI 的 `pendingEffect.skill.text` 確實是攻擊文字，串起轉換／hook／UI／規則四層驗證。
- 🌐 好友房 WebSocket 生命週期硬化：以單一有效連線防止舊 socket 覆寫新連線，CONNECTING 階段可安全離開，加入房間立即保留房號；新增 90 秒 Render 冷啟動與 10 秒首次回應 timeout、非預期 error/close、constructor/send 失敗、錯誤 JSON／GameState envelope 防護及合法私人協定 close code。新增 10 項 hook 回歸，並擴充雙瀏覽器 smoke 驗證伺服器無法連線時的錯誤提示與返回操作；測試基線升至 97 檔／1545 項。
- 🌐 新增本機雙瀏覽器好友房 Playwright smoke：啟動獨立 Vite 與權威 WebSocket server，驗證建房、加入、雙方開局、支援→主階段狀態同步及對手離線提示；納入 main push workflow，Playwright 驗證增至 5 套。
- 🧭 強化多段能力效果決策順序證據：新增 8 類 pending decision 表格回歸，驗證 `resolve-ability-effect` 無法繞過中途決策，並驗證看牌決策完成後可保留並恢復效果鏈；測試基線升至 96 檔／1535 項。
- 🧪 新增牌組編輯器 Playwright smoke：驗證錯誤 JSON 可恢復、合法 60 張牌組匯入／儲存，以及 1366×768／280×720 無水平溢出；並納入 main push 瀏覽器 workflow。
- 🛡️ R3 replay 完整化：AI `refresh-deck` 將可重播的 `shuffleSeed` 寫入 command payload，補上最後一個 AI Refresh 非種子洗牌缺口；新增 AI Refresh commandLog 重播回歸測試。
- 📚 P2 維護流程正式化確認完成（roadmap）：`release-process.md`、`card-update-process.md`、`regression-test-checklist.md`、`manual-playtest-checklist.md` 皆已存在且內容完整，本輪修正 `release-process.md` 寫死的「1449+ 項測試」為動態基線敘述（不得低於 Phase 0 基線，非固定數字）。
- 📚 README 大幅精簡（known-risks R12）：182 行縮減至約 80 行。重新審視 known-risks 時發現先前「CHANGELOG 從 README 抽出」（P2）並不完整——README 自留的「更新日誌」表格與 `CHANGELOG.md` 內容已分岔，9 筆歷史紀錄從未同步；已將分岔紀錄併入本檔（見下方 2026-07-11 補充項）、移除 README 重複表格，「目前進度」／「下一步計畫」改為短摘要 + 連結 `docs/architecture.md`／`docs/roadmap.md`／`docs/known-risks.md`。同時修正 `docs/known-risks.md` R4（`validate:cards` 早已存在並接入 CI，非「缺」）與 R8（缺圖 fallback 早已存在，非「待做」）兩處過時描述；`docs/architecture.md` 同步修正過時的 App.tsx 行數與測試數字。
- 📚 AI Lv.5 前置觀察（roadmap P3）：新增 `docs/ai-lv3-lv4-observation-2026-07-11.md`，以 7 場 Lv.3/Lv.4 對局逐字紀錄＋儀器化驗證（多重攻擊資源判定、陷阱使用頻率對照牌組組成）確認行為結構健康，建議暫緩 Lv.5 開發、待使用者真人對局確認後再決定。
- 📚 UI reference wireframe 文件（roadmap P3）：新增 `docs/ui-reference/06-online-match-wireframe.md`（線上對戰面板）；依實機驗證更新 `02-main-menu-wireframe.md`（空狀態 CTA 邏輯）；回填 `ui-audit-2026-07-11.md` 的 P0（主選單空狀態、線上對戰彈窗）已解決狀態。
- ⚡ 戰鬥資訊 modal 群組（`InformationModals`／`BattleResponseModals`／`DamageEffectModals`／`PendingDecisionModals`／`ResultModal`／`OpeningSetupModal`）改為 `React.lazy` + `Suspense`；主 bundle 由約 806.92 KB 降至 730.68 KB raw（167.17 → 152.26 KB gzip）。
- 🛡️ R3 指令層收尾：AI（`ai.ts`、`ai/battle-handler.ts`、`ai/turn-handler.ts`、`ai/random-turn-handler.ts`）全面改走 `applyGameCommand`，取代手動 `appendCommandLogEntry`；新增共用 `simulateAbilityEffects`（`src/game/ai/ability-effects.ts`）讓 AI 的 `play-item`／`activate-skill`／`activate-stage` 補齊 `effectTargets`，修復先前 AI 對局 commandLog 重播會失真的問題。過程中發現並修復 `commands.ts` 的 `assertNoPendingDecision` 缺口（補位帶出的新餅乾 OnPlay 未被排除在昏厥效果阻擋之外，會造成死結）。新增 `ai-replay-fidelity.test.ts`；測試基線升至 96 檔／1526 項。
- 🎨 P0 UI/UX 改進：線上對戰面板深藍電競科幻 modal 樣式化（`OnlineMatchPanel.tsx` + `GameModals.css`），含專屬 `.online-match-panel` 類別、32×32px 關閉按鈕、內容區捲軸管理、完整 hover/focus-visible/active/disabled 狀態。
- 🎨 P0 主選單空狀態引導：無自訂牌組時「建立第一副牌組」為 primary CTA、「對戰入口」disabled 並顯示原因文字；有牌組時還原以「對戰入口」為 primary CTA（`MainMenu.tsx` + `App.css`）。測試基線升至 92 檔／1488 項。新增 `OnlineMatchPanel.test.tsx`（15 項 mock hook 測試，含 idle/waiting/error/close/dialog/label/connecting 路徑）。
- 🔧 P1 工程管線（PR #24，驗證中）：`npm run validate:cards`（卡池 311 種卡號全數可轉換檢查，接入 CI）、`npm run typecheck`、`vercel.json` SPA rewrite、server 支援 `PORT` 環境變數與線上對戰冷啟動提示；修復 BS2-061@1 異圖版缺 level 的匯入資料缺陷並在匯入腳本加異圖回填。
- 📚 P2 維護流程文件（本 PR）：CHANGELOG 自 README 抽離、release / card-update 流程、回歸與手動測試清單、loop-engineering 說明。
- 🛡️ 攻擊宣告阻擋加固：`assertNoBlockingDecision` 新增 `pendingOnPlay` 與 `pendingAbilityEffect` 檢查，既有待處理效果結算完成前禁止宣告攻擊；新增 `battle-blocking-decision.test.ts` 回歸測試。
- 🛡️ AI 攻擊宣告與 determinism 修正：各級 AI 攻擊統一以 `declare-attack` 記入 commandLog 保留陷阱/FLIP 回應窗口；`commandLog` 長度不再影響 Lv.1 隨機決策；新增 `ai-attack-declaration.test.ts`（309 行），測試基線升至 91 檔／1469 項。
- 🔧 候選卡牌匯入管線強化：`validate:candidate` 新增 schemaVersion／source／欄位型別結構檢查；`promote:candidate` 加入檔名碰撞拒絕與 rollback；promote 後自動重新生成卡池 registry 確保新卡牌納入 runtime card pool。
- ✅ CI 新增 `validate:candidate` 與 `check:card-pool` gate，防止候選資料或 runtime registry 漏同步。
- 🌐 Vercel + Render 公網部署與雙視窗對局驗證完成。
- 🐛 修正線上對戰加入房間後對戰畫面未顯示（`MainMenu` 與 `OnlineBattleView` 兄弟元素重疊問題）。
- 🔧 指令出口統一補位排程：完成 `usePendingEffect` commandLog／replay 一致性與回歸測試。
- 📱 1194×680 平板解析度瀏覽器驗證完成：主選單、牌組編輯器、break-to-trash 對戰桌無 body 溢出，牌組編輯器 modal 在 viewport 內且操作列可見。
- ✅ AI 瀏覽器完整驗證 20/20 種子全綠（stuck=0）；互動／文案修正。
- 🔧 break-to-trash 結果訊息依有無目標分流；補齊 `effectUiUtils` 單元測試；同步 AI 瀏覽器斷言文案。
- 🐛 修正 `StatusToast` 訊息變更後未重新顯示；AI 瀏覽器測試的休息區卡牌點擊改為 DOM click，避免 modal backdrop 攔截。

## 2026-07-10

- 📋 Phase 0 文件與 IP 補強（PR #23）— 新增 audit-report、architecture、product-vision、roadmap、known-risks、ip-and-asset-policy、test-plan、online-server-hosting 文件；README 與主選單 footer 加非官方粉絲研究聲明；新增 MIT + Devsisters 素材除外條款的 LICENSE。

## 2026-07-09

- 🎨 EffectPanel 與陷阱/攻擊提示框改版（PR #20、#21）— dock、雙欄版面、多步驟流程；提示框加寬、背景加深。

## 2026-07-08

- 🐛 陷阱 support-to-hand/hand-to-support 修正 — 修復 Bean 牌組陷阱卡造成卡住的 bug；AI 改進支援放置能量稀缺優先、攻擊選擇一擊擊殺優先；新增 6 組 BS2 對局分析文件。
- 🔧 BS1-006 修正 — after-damage 觸發改為僅限戰鬥傷害，效果傷害不再觸發。
- 🔧 BS1-037 修正 — 移除 sourceAsEnergy 費用減少、目標改選 HP 最多、新增 hand-to-support 效果型別與執行。

## 2026-07-07

- 🃏 AI 預設牌組 — 新增第二彈紅/黃/豆子/藍/紫 AI 牌組選項；補強牌庫檢視縮小／返回、AI 支援階段填能與第二彈 5×5 對局矩陣回歸；記錄第二彈黃對紅 50 場策略訓練。
- 🛡️ AI 攻擊防守修正 — Lv.1/3/4 AI 攻擊改停在 trap 階段等人類防守方回應（先前自動結算導致無法使用陷阱/FLIP）。
- 🔧 BS1-037/054 修正 — MIX 區域顏色解析、BS1-054 OnPlay 廢棄判定、sourceAsEnergy 支付與 AI 決策聯動。

## 2026-07-06

- ✅ Phase 5 CI — 修正線上對戰 lint 失敗，維持 test/lint/build 通過。

## 2026-07-05

- 🧩 補齊卡牌效果（PR #17）— 稽核找出 25 張未支援卡，新增 8 個遊戲機制與 2 個代價/條件；修正 resolveFlip 條件檢查與異圖卡號正規化。

## 2026-07-04

- 🧠 AI 分級 — 新增 Lv.1/Lv.2、`PlayerView` 視角過濾器與 Lv.3 評估式 AI（PR #12、#13）。
- 🔗 指令層整合 — 擴充 `GameCommand`、加入 `commandLog` / replay，並補完牌組管理（PR #11）；對戰紀錄側欄（PR #15）；App.tsx 容器元件拆分（PR #16）。

## 2026-07-03

- 🖱️ Playwright 驗證 — 修正支援卡點擊、藍牌驗證斷言與瀏覽器測試流程。

## 2026-07-02

- 🟥 BS2 紅牌 — 完成 BS2-006/007 非餅乾效果、HP-to-trash 與紅色手牌代價。

## 2026-07-01

- 🧩 BS1/BS2 效果 — 補齊紅色卡牌、非餅乾效果、after-damage 與 attack-effect 控制權。

## 2026-06-30

- 🃏 BS1 匯入 — 建立 Brave Beginning Phase 1/2 轉接與測試基線。
